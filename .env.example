# Django
SECRET_KEY=change-me-to-a-random-secret-key
APP_PORT=8000
DEBUG=False
ALLOWED_HOSTS=mushoku.pegomon.online,localhost
CSRF_TRUSTED_ORIGINS=https://mushoku.pegomon.online

# CORS (comma-separated origins, only used when DEBUG=False)
CORS_ALLOWED_ORIGINS=https://mushoku.pegomon.online

# PostgreSQL (Neon)
PGHOST=your-neon-host.neon.tech
PGDATABASE=your_database
PGUSER=your_user
PGPASSWORD=your_password
PGPORT=5432
PGSSLMODE=require
